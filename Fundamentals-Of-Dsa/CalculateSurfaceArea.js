// Find surface area of sphere

let surfaceRadius = prompt('Enter the Surface radisu:')

let surfaceArea = (4 * Math.PI * parseFloat(surfaceRadius) * parseFloat(surfaceRadius)).toFixed(2)

console.log('The surface area of the sphere is: ',surfaceArea)